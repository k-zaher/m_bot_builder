require "spec_helper"

describe MBotBuilder do
  it "has a version number" do
    expect(MBotBuilder::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
